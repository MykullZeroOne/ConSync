# ConSync
onSync is a CLI tool that syncs local Markdown files with Confluence pages. It creates new pages if they don’t exist, updates existing ones, and preserves version history. Ideal for developers and teams who want automated, reliable documentation workflows.
